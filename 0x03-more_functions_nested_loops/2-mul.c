#include "holberton.h"

/**
 *mul - multiplies two digits, a and b
 *@a: first digit to be multiplied
 *@b: second digit to be multiplied
 *
 *Return: x, the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
